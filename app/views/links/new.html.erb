<%= flash[:error] %>
<%= simple_form_for @link do |f| %>
  <%= f.input :title %>
  <%= f.input :url %>
  <%= f.collection_select(:tags, Tag.order(:title), :formatted_id, :title, {prompt: true}, {class: "tag-selector form-control", multiple: "multiple"}) %>
  <%= f.button :submit %>
<% end %>
